package com.mycompany.fragmentoparanormal.util;

public enum TipoItem {
    /** Restaura pontos de vida. */
    CURA,
    /** Restaura pontos de esforço. */
    RESTAURAR_PE,
    /** Aumenta permanentemente o Poder Paranormal. */
    BOOST_PARANORMAL,
    /** Aumenta permanentemente a força física. */
    BOOST_FORCA,
    /** Aumenta permanentemente a vida máxima. */
    BOOST_VIDA_MAX,
    /** Artefato equipável que concede bônus passivos. */
    ARTEFATO,
    /** Item sem efeito ativo (relíquia/troféu). */
    COMUM
}
